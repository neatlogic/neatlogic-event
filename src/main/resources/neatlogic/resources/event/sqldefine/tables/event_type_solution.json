{
  "moduleId": "event",
  "name": "event_type_solution",
  "label": "事件类型-解决方案关联表",
  "description": "事件类型-解决方案关联表 可用于事件相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "event_type_id",
      "type": "bigint",
      "description": "事件类型ID，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "solution_id",
      "type": "bigint",
      "description": "解决方案ID，作为当前表记录的主键或唯一定位字段。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "event_type_id",
        "solution_id"
      ],
      "description": "主键索引，覆盖 event_type_id、solution_id 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "idx_event_type_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "event_type_id"
      ],
      "description": "普通索引，覆盖 event_type_id 字段，适合用于过滤、关联或排序查询。"
    },
    {
      "name": "idx_solution_id",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "solution_id"
      ],
      "description": "普通索引，覆盖 solution_id 字段，适合用于过滤、关联或排序查询。"
    }
  ]
}
